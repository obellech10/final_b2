<h1><%= @student.name %>'s Gradebook</h1>

<% @student.courses.each do |course| %>
  <section id='course-<%= course.id %>'>
    <%= course.name %>
    <%= course.student_courses.first.grade %>
  </section>
<% end %>
